/**
 * Enum to define the alignment of texts.
 */
export enum TextAlign {
    LEFT,
    RIGHT,
    CENTER
}
/**
 * Enum to define the style of the table.
 */
export enum TableStyle {
    SIMPLE,
    DEFAULT,
    NONE
}